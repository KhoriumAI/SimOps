# Core mesh generation
gmsh>=4.11.0
numpy>=1.24.0
scipy>=1.10.0

# GUI
PyQt5>=5.15.0
vtk>=9.2.0
pyvista>=0.40.0

# CAD file support (optional but recommended for STEP file preview)
pythonocc-core>=7.7.0

# GPU acceleration (optional - requires NVIDIA GPU + CUDA)
cupy-cuda12x>=12.0.0  # Use cupy-cuda11x for CUDA 11

# Utilities
requests>=2.28.0
psutil>=5.9.0

# Advanced features (optional)
pymesh2>=0.3.0  # Mesh repair and generation
tetgen>=0.6.0   # Alternative tetrahedral mesher

# Development tools (optional)
pytest>=7.2.0
black>=23.0.0
trimesh>=3.20.0
coacd>=1.0.0

# External Dependencies (System Level)
# - OpenFOAM v2006+ (via WSL2 on Windows) for Hex-Dominant Meshing
# - WSL2 for Windows users

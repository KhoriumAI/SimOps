# Scripts

This directory contains utility scripts, legacy tools, and one-off helpers.

**Note:** For structured testing and verification, see the **[verification_lab](../verification_lab)** directory.

## Contents
- Legacy meshing scripts.
- One-off utility scripts for specific tasks (e.g., `cleanup.py`).
- Standalone runners that haven't been integrated into the main `apps` or `cli`.
